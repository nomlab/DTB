<% if time_entry == TimeEntry.current %>
  <%= link_to (fa_icon "stop"),
      :controller => "time_entries",
      :action     => "stop" %>
<% else %>
  <%= link_to (fa_icon "play"),
      :id         => time_entry.id,
      :name       => time_entry.name,
      :task_id    => time_entry.task_id,
      :controller => "time_entries",
      :action     => "continue" %>
<% end %>
