<% if time_entry == TimeEntry.current %>
  <%= link_to '<i class="icon-stop"></i>'.html_safe,
      :id         => time_entry.id,
      :controller => "time_entries",
      :action     => "stop" %>
<% else %>
  <%= link_to '<i class="icon-play"></i>'.html_safe,
      :id         => time_entry.id,
      :name       => time_entry.name,
      :task_id    => time_entry.task_id,
      :controller => "time_entries",
      :action     => "continue" %>
<% end %>
